import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    loadingContainer: {
        display: 'flex', 
        position: 'absolute', 
        alignItems: 'center',
        justifyContent: 'center',
        zIndex: 1, 
        top: 0, 
        left: 0, 
        right: 0, 
        bottom: 0, 
        backgroundColor: '#000', 
        opacity: 0.7
    },
    loading: {
       
    }
})